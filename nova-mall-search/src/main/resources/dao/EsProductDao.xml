<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.mall.search.dao.EsProductDao">
    <resultMap id="esProductListMap" type="com.su.mall.search.domain.EsProduct" autoMapping="true">
        <id column="id" jdbcType="BIGINT" property="id" />
        <collection property="attrValueList" columnPrefix="attr_" ofType="com.su.mall.search.domain.EsProductAttributeValue" autoMapping="true">
            <id column="id" property="id" jdbcType="BIGINT"/>
        </collection>
    </resultMap>
    <select id="getAllEsProductList" resultMap="esProductListMap">
        select
            p.id,
            p.product_sn,
            p.brand_id,
            p.brand_name,
            p.product_category_id,
            p.product_category_name,
            p.pic,
            p.name,
            p.sub_title,
            p.price,
            p.sale,
            p.new_status,
            p.recommand_status,
            p.stock,
            p.promotion_type,
            p.keywords,
            p.sort,
            pav.id attr_id,
            pav.value attr_value,
            pav.product_attribute_id attr_product_attribute_id,
            pa.type attr_type,
            pa.name attr_name
        from pms_product p
        left join pms_product_attribute_value pav on p.id = pav.product_id
        left join pms_product_attribute pa on pav.product_attribute_id= pa.id
        where delete_status = 0 and publish_status = 1
        <if test="id!=null">
            and p.id=#{id}
        </if>
    </select>
</mapper>