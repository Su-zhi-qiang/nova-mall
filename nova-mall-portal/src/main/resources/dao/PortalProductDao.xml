<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.mall.portal.dao.PortalProductDao">
    <resultMap id="cartProductMap" type="com.su.mall.portal.domain.CartProduct" autoMapping="true">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <!-- 父类 PmsProduct 字段由 autoMapping 自动映射，删除冗余 <result> -->
        <collection property="productAttributeList" columnPrefix="attr_" ofType="com.su.mall.model.PmsProductAttribute" autoMapping="true">
            <id column="id" property="id"/>
            <!-- 商品属性字段由 autoMapping 自动映射 -->
        </collection>
        <collection property="skuStockList" columnPrefix="sku_" ofType="com.su.mall.model.PmsSkuStock" autoMapping="true">
            <id column="id" property="id"/>
            <!-- SKU库存字段由 autoMapping 自动映射 -->
        </collection>
    </resultMap>
    <resultMap id="promotionProductMap" type="com.su.mall.portal.domain.PromotionProduct" autoMapping="true">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <!-- 父类 PmsProduct 字段由 autoMapping 自动映射，删除所有冗余 <result> -->
        <collection property="skuStockList" columnPrefix="sku_" ofType="com.su.mall.model.PmsSkuStock" autoMapping="true">
            <id column="id" property="id"/>
            <!-- SKU库存字段由 autoMapping 自动映射 -->
        </collection>
        <collection property="productLadderList" columnPrefix="ladder_" ofType="com.su.mall.model.PmsProductLadder" autoMapping="true">
            <id column="id" property="id"/>
            <!-- 阶梯价格字段由 autoMapping 自动映射 -->
        </collection>
        <collection property="productFullReductionList" columnPrefix="full_" ofType="com.su.mall.model.PmsProductFullReduction" autoMapping="true">
            <id column="id" property="id"/>
            <!-- 满减价格字段由 autoMapping 自动映射 -->
        </collection>
    </resultMap>
    <select id="getCartProduct" resultMap="cartProductMap">
        SELECT
            p.id,
            p.name,
            p.sub_title,
            p.price,
            p.pic,
            p.product_attribute_category_id,
            p.stock,
            pa.id attr_id,
            pa.name attr_name,
            ps.id sku_id,
            ps.sku_code sku_code,
            ps.price sku_price,
            ps.stock sku_stock,
            ps.pic sku_pic
        FROM
            pms_product p
            LEFT JOIN pms_product_attribute pa ON p.product_attribute_category_id = pa.product_attribute_category_id
            LEFT JOIN pms_sku_stock ps ON p.id=ps.product_id
        WHERE
            p.id = #{id}
            AND pa.type = 0
        ORDER BY pa.sort desc
    </select>
    <select id="getPromotionProductList" resultMap="promotionProductMap">
        SELECT
            p.id,
            p.name,
            p.promotion_type,
            p.gift_growth,
            p.gift_point,
            sku.id sku_id,
            sku.price sku_price,
            sku.sku_code sku_sku_code,
            sku.promotion_price sku_promotion_price,
            sku.stock sku_stock,
            sku.lock_stock sku_lock_stock,
            ladder.id ladder_id,
            ladder.count ladder_count,
            ladder.discount ladder_discount,
            full_re.id full_id,
            full_re.full_price full_full_price,
            full_re.reduce_price full_reduce_price,
            fr.id flash_promotion_relation_id,
            fr.flash_promotion_id,
            fr.flash_promotion_session_id,
            fr.flash_promotion_price,
            fr.flash_promotion_count,
            fr.flash_promotion_limit
        FROM
            pms_product p
            LEFT JOIN pms_sku_stock sku ON p.id = sku.product_id
            LEFT JOIN pms_product_ladder ladder ON p.id = ladder.product_id
            LEFT JOIN pms_product_full_reduction full_re ON p.id = full_re.product_id
            LEFT JOIN sms_flash_promotion_product_relation fr ON p.id = fr.product_id
                AND fr.flash_promotion_count &gt; 0
                AND EXISTS (
                    SELECT 1 FROM sms_flash_promotion fp
                    WHERE fp.id = fr.flash_promotion_id
                    AND fp.status = 1
                    AND fp.start_date &lt;= CURDATE()
                    AND fp.end_date &gt;= CURDATE()
                )
                AND EXISTS (
                    SELECT 1 FROM sms_flash_promotion_session fs
                    WHERE fs.id = fr.flash_promotion_session_id
                    AND fs.status = 1
                    AND fs.start_time &lt;= CURTIME()
                    AND fs.end_time &gt;= CURTIME()
                )
        WHERE
            p.id IN
        <foreach collection="ids" open="(" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </select>
    <!-- 改为使用 resultType -->
    <select id="getAvailableCouponList" resultType="com.su.mall.model.SmsCoupon">
        SELECT *
        FROM sms_coupon
        WHERE use_type = 0
          AND start_time &lt; NOW()
          AND end_time &gt; NOW()
        UNION
        (
            SELECT c.*
            FROM sms_coupon_product_category_relation cpc
                     LEFT JOIN sms_coupon c ON cpc.coupon_id = c.id
            WHERE c.use_type = 1
              AND c.start_time &lt; NOW()
              AND c.end_time &gt; NOW()
              AND cpc.product_category_id = #{productCategoryId}
        )
        UNION
        (
            SELECT c.*
            FROM sms_coupon_product_relation cp
                     LEFT JOIN sms_coupon c ON cp.coupon_id = c.id
            WHERE c.use_type = 2
              AND c.start_time &lt; NOW()
              AND c.end_time &gt; NOW()
              AND cp.product_id = #{productId}
        )
    </select>
</mapper>
