<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.mall.dao.SmsCouponDao">
    <resultMap id="couponItemParam" type="com.su.mall.dto.SmsCouponParam" autoMapping="true">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <collection property="productRelationList" column="id" select="selectProductRelation" ofType="com.su.mall.model.SmsCouponProductRelation"/>
        <collection property="productCategoryRelationList" column="id" select="selectProductCategoryRelation" ofType="com.su.mall.model.SmsCouponProductCategoryRelation"/>
    </resultMap>

    <select id="getItem" resultMap="couponItemParam">
        SELECT c.*
        FROM sms_coupon c
        WHERE c.id = #{id}
    </select>

    <select id="selectProductRelation" resultType="com.su.mall.model.SmsCouponProductRelation">
        SELECT id, coupon_id, product_id, product_name, product_sn
        FROM sms_coupon_product_relation
        WHERE coupon_id = #{id}
    </select>

    <select id="selectProductCategoryRelation" resultType="com.su.mall.model.SmsCouponProductCategoryRelation">
        SELECT id, coupon_id, product_category_id, product_category_name, parent_category_name
        FROM sms_coupon_product_category_relation
        WHERE coupon_id = #{id}
    </select>
</mapper>
